# Quantumult X Release

Generated remote-filter payloads use Quantumult X rule names. Every rule contains placeholder policy direct; load with filter_remote and force-policy=YOUR_POLICY to override it. Unsupported semantics are emitted as *.unsupported.txt instead of being silently dropped.

Build summary: translated=381486, unsupported=191.
