{
  "routing": {
    "rules": [
      {
        "type": "field",
        "outboundTag": "proxy",
        "ruleTag": "ClientRuleSet non_ip/gitlab",
        "domain": [
          "domain:gitlab.com"
        ]
      }
    ]
  }
}
